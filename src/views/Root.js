import React from 'react';
import GlobalStyle from 'theme/GlobalStyle';
import Fetch from 'components/Fetch';

const Root = () => (
  <div>
    <GlobalStyle />
    <Fetch />
  </div>
);

export default Root;
